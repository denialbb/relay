# Relay

Keyboard-first unread Beeper triage drawer (Omarchy Quickshell `panel`).

> Who needs my attention right now, and can I handle it in one or two actions?

Full contract: `docs/relay_beeper_triage_spec.md`. Agent context: `AGENTS.md`.

## Status

M0 skeleton — red tests, no implementation yet.

## Dev

```bash
make check   # validate + lint + test + complexity
node --test tests/
```
