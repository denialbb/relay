# Group 1: UI & Styling

- in open chats there is a duplicated header with the chat name, integrate the both of them - keep the top one as it is better styled
- align to the right side the messages from the user in chats
- compress a bit the bottom help labels, they are overflowing outside the drawer width

Instructions: Fix these UI issues. Remember to run tests and keep code clean.
